"use client";

// import { useAppSelector } from "@/redux/store";

export default function Home() {
    // const initState = useAppSelector((state) => state.identity);
    // console.log(initState);
    return <div>Hello hear is React Application</div>;
}
